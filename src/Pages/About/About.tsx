import { Typography, Box } from '@mui/material';

export default function About() {
    return (
        <Box>
            <Typography variant="h4" gutterBottom>
                About Us now
            </Typography>
            <Typography variant="body1">
                Welcome to BrainTop - Your AI Learning Platform
            </Typography>
        </Box>
    );
}
